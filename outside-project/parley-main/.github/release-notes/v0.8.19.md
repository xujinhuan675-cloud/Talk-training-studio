## Fixes

- **Parley no longer asks for Accessibility permission on every launch.** The grant dialog now appears at most once (on first launch); if the permission is missing or was invalidated later, Parley degrades gracefully — dictation still copies to the clipboard — instead of nagging at startup. You can always grant or re-grant from Settings → Voice typing.
