# 通义千问 API Key（阿里云 DashScope）
QWEN_API_KEY=your-api-key-here

# real 使用真实 AI；mock 使用固定回复，适合本地无 Key 演示
AI_MODE=real

# 公开 demo 限流
AI_DAILY_LIMIT_PER_USER=30
AI_DAILY_LIMIT_PER_IP=80
AUTO_SEED_DEMO=1

# SQLite 数据库路径（默认：backend/ 目录下的 training_demo.db）
DB_PATH=training_demo.db
